databases:
  - name: budget
    databaseName: budget_5ykl
    user: budget_5ykl_user

services:
  - type: web
    name: moneyapps
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: budget_5ykl
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false